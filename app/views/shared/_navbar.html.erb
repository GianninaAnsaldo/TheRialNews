<nav class="navbar navbar-expand-lg bg-dark sticky-top" data-bs-theme="dark">
  <div class="container text-white">
    <%= link_to "TheRialNews", root_path, class:"navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <div class="nav-item">
         <%= link_to "Noticias", posts_path, class:"nav-link active", ariaCurrent:"page" %>
      </div>   
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <% if !user_signed_in? %>
                  Incia Sesión / Regístrate
              <% else %>
                  Hola <%= current_user.name %>
              <% end %>
        </a>
        <ul class="dropdown-menu">
              <% if !user_signed_in? %>
                  <li><%= link_to 'Iniciar Sesión', new_user_session_path, class:"dropdown-item" %></li>
                  <li><%= link_to 'Registrarse', new_user_registration_path, class:"dropdown-item" %></li>
              <% elsif %>
                  <li><%= link_to 'Mi Cuenta', edit_user_registration_path, class:"dropdown-item" %></li>
                  <li><%= button_to 'Cerrar Sesión', destroy_user_session_path, class:"dropdown-item", method: :delete, data: {turbo_confirm: "Estás seguro que deseas cerrar sesión?"} %></li>
              <% end %>
        </ul>
      </ul>
    </div>
  </div>
</nav>